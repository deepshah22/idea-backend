spring:
  jpa:
    show-sql: true

  data:
    redis:
      # Local dev — plain Redis from docker-compose, no TLS
      host: localhost
      port: 6379
      password:
      ssl:
        enabled: false  # overrides prod default; RedisConfig checks this flag

app:
  jwt:
    secret: dev-only-secret-key-not-for-production!!

logging:
  level:
    com.idea: DEBUG
    org.hibernate.SQL: DEBUG
